version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: deep_guard_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

  mongodb:
    image: mongo:7
    container_name: deep_guard_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: deep_guard
    restart: unless-stopped

volumes:
  redis_data:
  mongodb_data:
